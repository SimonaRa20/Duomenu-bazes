@model IEnumerable<Org.Ktu.Isk.P175B602.Autonuoma.ViewModels.DarbuotojasListVM>
@{
	ViewData["title"] = "Darbuotojai";
}

<div class="main-content-header container-fluid d-flex d-row justify-content-between align-items-center p-2">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("Create")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Sukurti naują</a>
	</span>
</div>

<div class="d-flex justify-content-center">
	<table class="entitylist table table-striped table-hover">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(m => m.Tabelio_nr)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Vardas)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Pavarde)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Pareigos)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Telefonas)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model) {
			<tr>
				<td>
					@Html.DisplayFor(m => item.Tabelio_nr)
				</td>
				<td>
					@Html.DisplayFor(m => item.Vardas)
				</td>
				<td>
					@Html.DisplayFor(m => item.Pavarde)
				</td>
				<td>
					@Html.DisplayFor(m => item.Telefonas)
				</td>
				<td>
					@Html.DisplayFor(m => item.Pareigos)
				</td>
				
				<td>
					<a href="@Url.Action("Edit", new { id = item.Tabelio_nr })" class="btn btn-primary"><i class="fa-solid fa-pen-to-square"></i> Redaguoti</a>
					<a href="@Url.Action("Delete", new { id = item.Tabelio_nr })" class="btn btn-danger"><i class="fa-solid fa-trash"></i> Šalinti</a>
				</td>
			</tr>
			}
		</tbody>
	</table>
</div>

@model IEnumerable<Org.Ktu.Isk.P175B602.Autonuoma.ViewModels.KnygaListVM>
@{
	ViewData["title"] = "Knygos";
}

<div class="main-content-header container-fluid d-flex d-row justify-content-between align-items-center p-2">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("Create")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Sukurti naują</a>
	</span>
</div>

<div class="d-flex justify-content-center">
	<table class="entitylist table table-striped table-hover">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(m => m.ISBN)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Pavadinimas)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Autorius)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Leidykla)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Zanras)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model) {
			<tr>
				<td>
					@Html.DisplayFor(m => item.ISBN)
				</td>
				<td>
					@Html.DisplayFor(m => item.Pavadinimas)
				</td>
				<td>
					@Html.DisplayFor(m => item.Autorius)
				</td>
				<td>
					@Html.DisplayFor(m => item.Leidykla)
				</td>
				<td>
					@Html.DisplayFor(m => item.Zanras)
				</td>
				<td>
					<a href="@Url.Action("Edit", new { id = item.ISBN })" class="btn btn-primary"><i class="fa-solid fa-pen-to-square"></i> Redaguoti</a>
					<a href="@Url.Action("Delete", new { id = item.ISBN })" class="btn btn-danger"><i class="fa-solid fa-trash"></i> Šalinti</a>
				</td>
			</tr>
			}
		</tbody>
	</table>
</div>
